# Core typing & config
pydantic==2.12.0
python-dotenv==1.1.1

# HTTP
requests==2.32.3

# System/resource insights
psutil==5.9.8

# Media playback
python-vlc==3.0.20123

# Filename parsing
guessit==3.7.1
subliminal>=2.2.0
chardet>=5.0,<6.0

# File system watching
watchdog>=3.0.0

# Fuzzy string matching (torrent name ranking)
thefuzz==0.22.1

# Async HTTP (torrent search, RealDebrid API)
aiohttp==3.11.12

# FastAPI (API layer + SSE endpoints)
fastapi==0.115.6
uvicorn==0.34.0

# Local torrent download (for RD-blocked sources)
libtorrent>=2.0.0

# Desktop UI
PySide6>=6.6.0
